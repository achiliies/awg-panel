"""Django project package: settings, URL map, middleware and the SPA shell.

Everything that knows about HTTP lives here or in ``apps/``. The VPN itself is
managed through ``awg/``, which imports no Django and can be driven from a
plain shell.
"""
